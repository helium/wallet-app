query Account($address: String!) {
  account(address: $address) {
    address
    balance
    block
    dcBalance
    dcNonce
    nonce
    secBalance
    secNonce
    speculativeNonce
    speculativeSecNonce
    stakedBalance
    mobileBalance
    iotBalance
  }
}
