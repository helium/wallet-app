query Notifications($address: String!, $resource: String!) {
  notifications(address: $address, resource: $resource) {
    resource
    body
    icon
    title
    type
    id
    viewedAt
    time
    actionTitle
    actionUrl
  }
}
