query stripeParams(
  $address: String!
  $amount: Int!
  $burnTxn: [Int!]!
  $customerId: String
) {
  stripeParams(
    address: $address
    amount: $amount
    burnTxn: $burnTxn
    customerId: $customerId
  ) {
    customerId
    ephemeralSecret
    paymentSecret
    burnSignature
  }
}
