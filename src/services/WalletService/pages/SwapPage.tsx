import SwapNavigator from '@features/swaps/SwapNavigator'
import { ThemeProvider } from '@shopify/restyle'
import { darkTheme } from '@theme/theme'
import React from 'react'

const SwapPage = () => {
  return <SwapNavigator />
}

export default SwapPage
